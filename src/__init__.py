# This file makes the 'src' directory a Python package.
# It allows 'app.py' to import functions from 'ocr_engine' and 'llm_engine'.

# You can leave this file empty, or you can use it to expose functions directly.
# For this project, keeping it empty (or just this comment) is perfectly fine.